# We could have one .gitignore in the root path which could handle all unneccessary file
# Since git ignores empty folders so we need to put this .gitignore file here so that our OBJ folder no more remains empty 

# Ignore all .o files in this directory
*.o